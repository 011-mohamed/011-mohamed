{% extends "base.html.twig" %}
{% block title %}

	{{ad.title}}

{% endblock %}

{% block body %}
	<div class="title" style="background-image:url({{asset(ad.coverImage)}})">
		<div class="container">
			<h1>{{ad.title}}</h1>
			<p>{{ad.intoduction}}</p>
			<p class="h2">
				<strong>{{ad.rooms}}
					chambres</strong>
				pour
				<strong>{{ad.price}}
					TND</strong>
				par nuit
			</p>
			<a href="#" class="btn btn-dark">Reserver</a>
		</div>
	</div>
	<div class="container ">
		<div class="row ">
			<div class="col-md-8 mb-3">
				{{ ad.content | raw }}
				<div id="carouselExampleIndicators" class="carousel slide" data-bs-ride="carousel">
					<ol class="carousel-indicators">
						{% for image in ad.images %}
							<li data-bs-target="#carouselExampleIndicators" data-bs-slide-to="{{ loop.index0 }}" {% if loop.first %} class="active" {% endif %}></li>
						{% endfor %}
					</ol>
					<div class="carousel-inner">
						{% for image in ad.images %}
							<div class="carousel-item {% if  loop.first %} active {% endif %}">
								<img src="{{asset(image.url)}}" class="d-block w-100" alt="...">
								<div class="carousel-caption d-none d-md-block">
									<h5>{{ ad.title}}</h5>
									
									<p>{{ image.caption }}</p>
								</div>
							</div>
						{% endfor %}
					</div>
					<a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-bs-slide="prev">
						<span class="carousel-control-prev-icon" aria-hidden="true"></span>
						<span class="visually-hidden">Previous</span>
					</a>
					<a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-bs-slide="next">
						<span class="carousel-control-next-icon" aria-hidden="true"></span>
						<span class="visually-hidden">Next</span>
					</a>
				</div>


			</div>
			<div class="col">
				<div class="row">
					<div class="col-4">
						<img src="http://placeholder.it/500*88" alt="Avatar de">
					</div>
					<div class="col">
						<h3>Khalil Salem</h3>
						<span class="badge badge-primary">3 annnonce</span>
					</div>
				</div>
				<p>Lorem ipsum dolor sit amet consectetur adipisicing elit. Maxime perferendis est consectetur numquam! Facere cumque veritatis quisquam ipsa? Vel, voluptas.</p>
				<p>Maiores maxime porro amet totam rem assumenda laboriosam alias quasi laudantium earum a soluta veritatis, veniam eveniet accusamus nisi nobis?</p>
				<p>Quaerat, deleniti ullam amet explicabo dolore repellendus sit error consequatur officiis ipsa obcaecati delectus consectetur quasi dicta mollitia commodi necessitatibus.</p>
			</div>
		</div>
	</div>


{% endblock %}
