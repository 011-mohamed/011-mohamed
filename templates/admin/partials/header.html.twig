
<nav class="navbar navbar-expand-lg navbar-dark bg-success">
	<a class="navbar-brand" href="{{path('homepage')}}">Tounsi100%</a>
	
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarColor01">
		<ul class="navbar-nav mr-auto">
			<li class="nav-item ">
				<a class="nav-link" href="#">Dashboard</a>
			</li>
			<li class="nav-item ">
				<a class="nav-link" href="{{path('admin_ads_index')}}">Annonces</a>
			</li>
			<li class="nav-item ">
				<a class="nav-link" href="{{path('admin_booking_index')}}">Réservations</a>
			</li>
			<li class="nav-item ">
				<a class="nav-link" href="{{path('admin_comment_index')}}">Commentaires</a>
			</li>
			<li class="nav-item ">
				<a class="nav-link" href="#">Utilisateur</a>
			</li>
			
		</ul>
		<ul class="navbar-nav ml-auto">
			<li class="nav-item ">
				<a class="nav-link" href="{{path('homepage')}}">Voir le site</a>
			</li>

			{% if app.user %}
				<li class="nav-item active">
					<a href="{{path('account_index')}}" class="nav-link " >
						<img src="{{app.user.picture}}" class="avatar avatar-mini mr-2" alt="Avatar de{{app.user.firstName}}{{app.user.lastName}}">{{app.user.firstName}} {{app.user.lastName}}
					</a>
				</li>
				<li class="nav-item active" >
					<a href="{{path('admin_account_logout')}}" class="nav-link">Déconnexion</a>
				</li>
			
			{% endif %}
		</ul>
	</div>
</nav>
{% for label, messages in app.flashes  %}
	<div class="container">
		<div class="alert alert-{{label}}">
			{% for message in messages %}
				<p>{{message | raw}}</p>
			{% endfor %}
		</div>
	</div>
{% endfor %}
